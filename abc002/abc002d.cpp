#include <iostream>
#include <vector>

using namespace std;

int main(){
    int n;cin >> n;
    int m;cin >> m;
    vector<vector<int> > in(m,vector<int>(2));
    for(int i=0;i<m;i++){
        cin >> in[i][0];
        cin >> in[i][1];
    }
    for(int i=0;i<(1<<n);i++){
        
    }
}